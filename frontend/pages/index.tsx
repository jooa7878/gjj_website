import { css, jsx } from '@emotion/react';

export default function Home() {
  return (
    <div
      css={css({
        backgroundColor: 'black',
        color: 'orange',
      })}
    >
      Hello World
    </div>
  );
}
