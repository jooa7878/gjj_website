import { css } from '@emotion/react';

const container = css`
  display: flex;
  align-items: center;
  position: relative;
`;

const button = css`
  position: absolute;
  top: 15px;
  right: 10px;
`;

const styles = { container, button };

export default styles;
